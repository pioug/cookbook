# Recipe for Puppeteer in a Docker container for Google Cloud Run

Bonus:

- Node 14 (Alpine Linux)
- Chromium from Alpine Linux package management
- Random user agent to bypass Cloudflare Browser Integrity Check
